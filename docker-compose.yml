services:
  backend:
    build: .
    container_name: flynt-server
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - ENV=production
    env_file:
      - .env
    networks:
      - app-network
